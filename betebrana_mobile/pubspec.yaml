name: betebrana_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  dio: ^5.3.3
  retrofit: ^4.0.1
  json_annotation: ^4.8.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  encrypt: ^5.0.1
  flutter_staggered_animations: ^1.1.1
  liquid_pull_to_refresh: ^3.0.1
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.0
  connectivity_plus: ^5.0.1
  pull_to_refresh: ^2.0.0
  font_awesome_flutter: ^10.5.0
  
  # UPDATED: Use compatible versions
  file_picker: ^10.3.7
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  flutter_secure_storage: ^9.2.4
  
  # Web-specific packages
  universal_html: ^2.2.4
  url_launcher: ^6.1.17
  http: ^1.2.0

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  
  # UPDATED: Compatible versions for retrofit
  build_runner: ^2.4.7
  retrofit_generator: ^10.0.0  # UPDATED: Compatible with retrofit 4.x
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
assets:
  - lib/features/auth/image/unnamed.jpg
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"